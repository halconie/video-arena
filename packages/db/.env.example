# Read by the Prisma CLI when running `bun run db:migrate` / `db:generate`
# / `db:studio` directly from this package. Copy to `.env`.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/video_arena
